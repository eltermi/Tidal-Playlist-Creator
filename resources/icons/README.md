`app.png` is the source artwork provided for the application.

`app.icns` contains the macOS icon sizes used automatically by `build_mac.sh`.

`app.ico` contains the Windows icon sizes used automatically by
`build_windows.ps1`.
